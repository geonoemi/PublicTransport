import java.io.BufferedReader;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.FileReader;
import java.io.IOException;

public class DirectoryTry {
/*
public static void readIn(String directory) throws IOException{ 
	try {
	//how to read in a directory ?
	//File directory=new File("directory");
	//File files[];
		
		FileReader reader=new FileReader(directory);
		BufferedReader buffer=new BufferedReader(reader);
		int i=0;
		
		
			if(directory.isDirectory()) {
				for(i=0;i<directory.size();i++) {
					
				}
				String[]contentOfDirectory=directory.list();
				files=new File[contentOfDirectory.length];
			
				for( i=0;i<files.length;i++) {
					files[i]=new File(directory.getAbsolutePath()+"/"+contentOfDirectory[i]);
				}
				for(String c:contentOfDirectory) {
					System.out.println(c);
				}
			}	
		
	}catch(FileNotFoundException e) {
		System.out.println("File not found.");
	}catch(IOException e) {
		System.out.println("e.getMessage()");
	}
}

	
	public static void main(String[] args) {
		readIn("C:\\Users\\geono\\eclipse-workspace\\Tomegkozlekedes\\classes files");
	}
*/
}
